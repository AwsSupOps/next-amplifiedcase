/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
 // basePath:'/blogs'/// revoming this path
 basePath:'/line' /// revoming this path
}

module.exports = nextConfig
